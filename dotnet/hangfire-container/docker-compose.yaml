---

version: "3.9"
services:
  dashboard:
    build:
      context: .
      dockerfile: ./HangfireContainer.Dashboard/dockerfile
    image: hangfire-container-dashboard:latest
  server:
    build:
      context: .
      dockerfile: ./HangfireContainer.Server/dockerfile
    image: hangfire-container-server:latest
